name: 🐛 Bug translated / Bug report
description: translated featuretranslated、translated、translated / Something is broken
labels: ["bug", "needs-triage"]
body:
  - type: markdown
    attributes:
      value: |
        translated。translated'sinfo，translatedissue；translatedversion / translated / model's issue translatedprocess。
        Thanks for reporting. Version, platform and model are required for us to reproduce.

  - type: dropdown
    id: platform
    attributes:
      label: translated / Platform
      options:
        - macOS translated (Apple Silicon)
        - Windows translated (x64)
        - Docker
        - translated / From source
    validations:
      required: true

  - type: input
    id: version
    attributes:
      label: AutoClip version / Version
      description: Settings pagetranslatedor「translated」translatedcantranslated / Shown in Settings → About
      placeholder: "v1.2.1"
    validations:
      required: true

  - type: input
    id: model
    attributes:
      label: use'smodelProvidesprovider / LLM provider
      placeholder: "translatedif translated qwen-plus / OpenAI gpt-4o / translated / translated OpenAI translated"
    validations:
      required: true

  - type: dropdown
    id: source
    attributes:
      label: videotranslated / Video source
      options:
        - Bsitetranslated / Bilibili
        - YouTube
        - localfile / Local file
        - translated / N/A
    validations:
      required: true

  - type: textarea
    id: what
    attributes:
      label: translated / What happened
      description: translated、translated、translated / Steps, expected, actual
      placeholder: |
        1. translated https://…
        2. click「translated」
        3. progresstranslatedin「translated 63%」translated / translated xxx
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: logs / translated / Logs or screenshots
      description: |
        translatedlogstranslated：macOS `~/Library/Application Support/AutoClip/logs`，Windows `%APPDATA%\AutoClip\logs`。translated 50 translatedcan，translated API Key。
      render: text
